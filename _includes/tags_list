{% for tag in site.tags %} 
  {% if tag[1].size > 1 %}
  	<a href="{{ BASE_PATH }}{{ site.JB.tags_path }}#{{ tag[0] }}">{{ tag[0] }} <span class="badge">{{ tag[1].size }}</span></a>&nbsp;&nbsp;&nbsp;
  {% endif %}
{% endfor %}
